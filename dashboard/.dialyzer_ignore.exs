# Dialyzer ignore file
# Add patterns here when you want to suppress specific warnings, for example:
#
# {"lib/some_file.ex", :pattern_match, 42}
# {"lib/some_file.ex", :call_without_opaque, 17}

# Currently empty - we want to see real issues.
