# .env.example
# Copy to .env and adjust for local/dev
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=

# For future Phoenix + cluster
PHX_HOST=localhost
SECRET_KEY_BASE=pleasechangemeinprod

# libcluster
CLUSTER_STRATEGY=gossip
# or epmd for local multi-node

# Simulator defaults
SIM_DEVICE_COUNT=3
SIM_PUBLISH_INTERVAL_MS=2500
